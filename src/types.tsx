export interface Todo {
  id: number;
  todo: string;
  isDone: boolean;
}

export interface Projects {
  name: string;
  discription: string;
  stack: string[];
  link: string;
  git: string;
  img: string;
}
